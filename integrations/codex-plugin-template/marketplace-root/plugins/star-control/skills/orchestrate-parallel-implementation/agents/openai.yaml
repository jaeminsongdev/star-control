interface:
  display_name: "Parallel Implementation"
  short_description: "명시 승인된 Sol/Terra Codex App thread/worktree 병렬 구현"
  default_prompt: "Use $orchestrate-parallel-implementation to implement this request with maximal safe parallelism."

policy:
  allow_implicit_invocation: true
