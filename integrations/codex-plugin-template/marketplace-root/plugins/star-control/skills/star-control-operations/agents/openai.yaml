interface:
  display_name: "Star-Control Operations"
  short_description: "Route development and code health through Star-Control"
  default_prompt: "Use $star-control-operations to plan, execute, verify, or update this development task through the correct Star-Control route."

dependencies:
  tools:
    - type: "mcp"
      value: "star-control"
      description: "Installed Star-Control fixed MCP gateway"

policy:
  allow_implicit_invocation: true
