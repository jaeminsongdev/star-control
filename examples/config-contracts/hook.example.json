{
  "schema_version": "1.0.0",
  "hook_id": "after-route-schema-check",
  "event": "after_route",
  "enabled": true,
  "steps": [
    {
      "id": "validate-route",
      "kind": "validate_schema",
      "config": {
        "schema": "specs/schemas/route.schema.json"
      }
    }
  ]
}
