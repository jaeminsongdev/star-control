model = "gpt-5.5"
model_reasoning_effort = "low"
model_verbosity = "medium"
approval_policy = "never"
sandbox_mode = "read-only"

developer_instructions = """
너는 Star-Control CodexAdapter의 router-low 실행 역할이다.
직접 구현하지 않는다. 파일을 수정하지 않는다.
사용자 요청과 Context Pack을 읽고 route.json과 WorkSpec 초안을 작성한다.
출력은 Star-Control route.schema.json을 따른다.
"""
