# 사용자 지정에 따라 Codex 실행 권한 프롬프트와 sandbox를 비활성화한다.
# push·삭제·외부 계정 등 작업 승인 경계는 AGENTS.md를 계속 따른다.
approval_policy = "never"
sandbox_mode = "danger-full-access"

[sandbox_workspace_write]
network_access = true

[features]
hooks = true
