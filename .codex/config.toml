# 사용자가 고정한 무프롬프트·full-access 기본값이다. 명백한 파괴 행위는 Rules/Hook이 deny한다.
approval_policy = "never"
sandbox_mode = "danger-full-access"

[sandbox_workspace_write]
network_access = true

[features]
hooks = true
