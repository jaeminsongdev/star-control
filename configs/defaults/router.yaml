schema_version: 0.1.0
route_policy:
  default_stages:
    - route
    - plan
    - implement
    - validate
    - review
    - report
  provider_selection:
    use_capabilities: true
    require_policy_check: true
