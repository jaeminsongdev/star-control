id: my-local-process
provider: provider.local-process
enabled: true
limits:
  timeout_seconds: 300
  max_parallel_jobs: 1
routing_tags:
  - local
  - process
command:
  executable: runner
  model_path: model.gguf
