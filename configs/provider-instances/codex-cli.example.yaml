id: my-codex-cli
provider: provider.codex-cli
enabled: true
limits:
  timeout_seconds: 300
  max_parallel_jobs: 1
routing_tags:
  - cloud
  - coding-agent
  - file-edit
command:
  executable: codex
