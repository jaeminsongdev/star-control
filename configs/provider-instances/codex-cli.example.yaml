id: my-codex-cli
provider: provider.codex-cli
enabled: true
limits:
  timeout_seconds: 300
  max_parallel_jobs: 1
routing_tags:
  - cloud
  - coding-agent
  - file-edit
transport_config:
  auth_mode: login_session
  privacy_handoff_approved: false
command:
  executable: codex
