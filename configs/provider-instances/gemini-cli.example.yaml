id: my-gemini-cli
provider: provider.gemini-cli
enabled: true
limits:
  timeout_seconds: 300
  max_parallel_jobs: 1
routing_tags:
  - cloud
  - coding-agent
transport_config:
  auth_mode: login_session
  privacy_handoff_approved: false
command:
  executable: gemini
