id: my-local-openai-compatible
provider: provider.local-openai-compatible
enabled: true
limits:
  timeout_seconds: 300
  max_parallel_jobs: 1
routing_tags:
  - local
  - private
endpoint:
  base_url: "http://127.0.0.1:8000/v1"
  model: local-coder
