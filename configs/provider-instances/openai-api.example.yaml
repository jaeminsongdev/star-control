id: my-openai-api
provider: provider.openai
enabled: true
limits:
  timeout_seconds: 300
  max_parallel_jobs: 1
routing_tags:
  - cloud
  - api
endpoint:
  base_url: "https://api.openai.com/v1"
  model: gpt-example
