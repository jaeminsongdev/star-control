version: 1
selection_order:
  router-low:
    - codex
    - local-ollama
  worker-impl:
    - codex
  worker-review:
    - codex
    - local-ollama
  worker-local-draft:
    - local-ollama
fallback_policy:
  provider_unavailable: try_next
  schema_failure: retry_once_then_fail
  permission_denied: mark_needs_approval
