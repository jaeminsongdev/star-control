version: 1
default:
  network_access: false
  writable_roots:
    - project_root
    - run_dir
  deny_paths:
    - "%USERPROFILE%/.ssh"
    - "%USERPROFILE%/.aws"
    - "%USERPROFILE%/.git-credentials"
    - "%USERPROFILE%/.config/gh"
