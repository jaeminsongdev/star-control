case_id: p0-scope-violation-001
rule_id: task.scope.allowed_paths
should_fail: true
input:
  task_id: p0-scope-demo
  allowed_paths:
    - "src/allowed/**"
  changed_files:
    - "src/allowed/index.ts"
    - "src/other/hidden.ts"
expected:
  severity: block
  decision: BLOCK
  message_contains: "allowed_paths"
notes: "허용 범위 밖 파일 변경은 P0에서 차단한다."
